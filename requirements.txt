# ImpactX Training Studio — Lean Optimized (Python 3.13)
# Streamlit UI + LLM + video pipeline (MoviePy) + imageio fallback + TTS

streamlit==1.34.0
pandas==2.2.2
openai==1.30.0
pillow==10.4.0
numpy==2.1.3
